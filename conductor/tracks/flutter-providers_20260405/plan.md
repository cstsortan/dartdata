# Implementation Plan: Flutter Provider Integration

**Track ID:** flutter-providers_20260405
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-05
**Status:** [ ] Pending | [~] In Progress | [x] Complete

## Overview

Create `ModelContainerProvider` as an `InheritedWidget` and add `ModelContext.of(BuildContext)` as a static lookup method. Simple, focused track — two classes and a static method.

---

## Phase 1: ModelContainerProvider

### Tasks

- [ ] Task 1.1: **Red** — Write widget test: `ModelContainerProvider` makes the container accessible to descendants via `ModelContainerProvider.of(context)`.
- [ ] Task 1.2: **Green** — Implement `ModelContainerProvider` extending `InheritedWidget` with a `container` field and static `of(BuildContext)` method.
- [ ] Task 1.3: **Red** — Write test: calling `ModelContainerProvider.of(context)` without a provider in the tree throws a descriptive `StateError`.
- [ ] Task 1.4: **Green** — Implement the error case with a clear message.
- [ ] Task 1.5: **Red** — Write test: `updateShouldNotify` returns true when container reference changes.
- [ ] Task 1.6: **Green** — Implement `updateShouldNotify` with reference comparison.

### Verification

- [ ] Provider makes container accessible in widget tree
- [ ] Missing provider throws clear error

---

## Phase 2: ModelContext.of(BuildContext)

### Tasks

- [ ] Task 2.1: **Red** — Write test: `ModelContext.of(context)` returns a working `ModelContext` connected to the provider's container.
- [ ] Task 2.2: **Green** — Implement `ModelContext.of(BuildContext)` as a static method: look up `ModelContainerProvider.of(context)`, create `ModelContext(container)`.
- [ ] Task 2.3: **Red** — Write test: can insert, save, and fetch using the context obtained from `ModelContext.of()`.
- [ ] Task 2.4: **Green** — Verify full CRUD works through the provider chain.
- [ ] Task 2.5: **Red** — Write test: `ModelContext.of()` without a provider throws a descriptive error.
- [ ] Task 2.6: **Green** — Delegate to the provider's error handling.

### Verification

- [ ] Full round-trip works via provider chain
- [ ] `flutter test` — zero failures

---

## Phase 3: Lifecycle and Export

### Tasks

- [ ] Task 3.1: **Red** — Write test: `ModelContainer.dispose()` closes the database connection; subsequent operations throw.
- [ ] Task 3.2: **Green** — Implement `dispose()` on `ModelContainer` that closes the SQLite connection.
- [ ] Task 3.3: Export `ModelContainerProvider` from `dartdata.dart` barrel file.
- [ ] Task 3.4: Verify all existing tests still pass (no import conflicts).

### Verification

- [ ] Container lifecycle properly managed
- [ ] `flutter test` — all tests pass, zero failures
- [ ] Public API exports updated

---

## Final Verification

- [ ] All acceptance criteria from spec met
- [ ] Provider + context lookup works end-to-end
- [ ] `flutter test` passes with zero failures

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._

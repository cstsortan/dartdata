# Implementation Plan: Generator — Relationship Emission

**Track ID:** generator-relationships_20260405
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-05
**Status:** [ ] Pending | [~] In Progress | [x] Complete

## Overview

Extend the generator to read `@relationship` annotations, infer cardinality from the field type, emit FK columns and junction tables, and populate `RelationshipDefinition` entries in the generated descriptor.

---

## Phase 1: To-One Relationship Detection

Handle the simplest case: a single related model field generates an FK column.

### Tasks

- [ ] Task 1.1: **Red** — Write generator test: a field `LivingAccommodation? accommodation` with `@relationship()` produces a `RelationshipDefinition` with `cardinality: toOne` and emits an FK column `accommodation_id INTEGER`.
- [ ] Task 1.2: **Green** — Implement `_readRelationshipAnnotation(FieldElement)` that detects `@relationship` and reads `deleteRule`, `inverse`. Determine cardinality from field type (`T?` → toOne optional, `T` → toOne required, `List<T>` → toMany).
- [ ] Task 1.3: **Red** — Write test: required to-one field `Trip trip` emits a non-nullable FK column.
- [ ] Task 1.4: **Green** — Handle nullable vs non-nullable in FK column generation.
- [ ] Task 1.5: **Red** — Write test: `deleteRule: DeleteRule.cascade` is stored in the generated `RelationshipDefinition`.
- [ ] Task 1.6: **Green** — Emit `deleteRule` from the annotation into the generated code.

### Verification

- [ ] Generator correctly emits FK columns for to-one relationships
- [ ] `RelationshipDefinition` entries are populated with correct metadata

---

## Phase 2: To-Many Relationships

Handle one-to-many: the FK column goes on the child table, not the parent.

### Tasks

- [ ] Task 2.1: **Red** — Write test: `@relationship() List<BucketListItem> bucketList` on Trip does NOT add a column to Trip; instead the FK is on BucketListItem.
- [ ] Task 2.2: **Green** — For `List<T>` fields, emit a `RelationshipDefinition` with `cardinality: toMany` but do NOT add a column to the parent table (the child table's to-one side owns the FK).
- [ ] Task 2.3: **Red** — Write test: `@relationship(inverse: 'bucketList') Trip? trip` on BucketListItem references the inverse correctly.
- [ ] Task 2.4: **Green** — Wire `inverse` field name into the generated `RelationshipDefinition`.
- [ ] Task 2.5: **Refactor** — Ensure both sides of a relationship produce complementary `RelationshipDefinition` entries.

### Verification

- [ ] To-many relationships don't add columns to the parent table
- [ ] Inverse wiring is correct on both sides

---

## Phase 3: Many-to-Many Junction Tables

Handle the case where both sides declare `List<T>`.

### Tasks

- [ ] Task 3.1: **Red** — Write test: Movie with `@relationship(inverse: 'movies') List<Actor> actors` and Actor with `List<Movie> movies` generates a junction table `_actor_movie` with two FK columns.
- [ ] Task 3.2: **Green** — Detect many-to-many: both sides have `List<OtherType>` + one side has `inverse`. Generate `CREATE TABLE _<a>_<b>` with two `INTEGER REFERENCES` columns and a composite PRIMARY KEY.
- [ ] Task 3.3: **Red** — Write test: explicit junction model (`MovieRole` with two `@relationship` fields) does NOT generate a hidden junction table.
- [ ] Task 3.4: **Green** — Skip junction table generation when the `List<T>` target type is itself a `@model` with relationship fields pointing back to both parents.
- [ ] Task 3.5: **Red** — Write test: junction table name uses alphabetical order of the two table names.
- [ ] Task 3.6: **Green** — Sort table names alphabetically for consistent junction table naming.

### Verification

- [ ] Junction tables generated for simple many-to-many
- [ ] No junction table for explicit junction models
- [ ] Consistent naming convention

---

## Phase 4: Integration and Generated Output Validation

### Tasks

- [ ] Task 4.1: **Red** — Write full integration test: generate a complete model file with mixed relationship types, verify the full output.
- [ ] Task 4.2: **Green** — Fix any issues found during full output validation.
- [ ] Task 4.3: **Refactor** — Clean up generated code formatting; ensure `RelationshipDefinition` entries are well-structured.
- [ ] Task 4.4: Verify that the generated output matches the format of the hand-written test models in `test_models.dart`.

### Verification

- [ ] Generated descriptors match the hand-written test model format
- [ ] `flutter test` in dartdata — zero failures
- [ ] All relationship types covered: to-one, to-many, many-to-many

---

## Final Verification

- [ ] All acceptance criteria from spec met
- [ ] Generator emits FK columns, junction tables, and RelationshipDefinitions
- [ ] `flutter test` passes with zero failures in both packages

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._

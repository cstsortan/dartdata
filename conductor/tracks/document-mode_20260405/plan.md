# Implementation Plan: Document Mode

**Track ID:** document-mode_20260405
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-05
**Status:** [ ] Pending | [~] In Progress | [x] Complete

## Overview

Wire `ModelConfiguration.document(directory:)` to place the database as `StoreContent` inside the directory, co-locate blob storage, and handle directory creation/WAL checkpoint on close.

---

## Phase 1: Database in Document Directory

### Tasks

- [ ] Task 1.1: **Red** — Write test: `ModelConfiguration.document(directory: dir)` creates a `StoreContent` file inside `dir`.
- [ ] Task 1.2: **Green** — Implement the document mode path resolution in `ModelContainer.create()`: set DB path to `<dir>/StoreContent`.
- [ ] Task 1.3: **Red** — Write test: the directory is created automatically if it doesn't exist.
- [ ] Task 1.4: **Green** — Add `directory.createSync(recursive: true)` before opening the database.
- [ ] Task 1.5: **Red** — Write test: insert data, close container, reopen from same directory — data persists.
- [ ] Task 1.6: **Green** — Ensure standard open/close lifecycle works with document path.

### Verification

- [ ] Document directory contains `StoreContent` database
- [ ] Data persists across open/close cycles

---

## Phase 2: Co-located Blob Storage

### Tasks

- [ ] Task 2.1: **Red** — Write test: inserting a model with ExternalFile in document mode stores the blob inside `<dir>/_EXTERNAL_DATA/`.
- [ ] Task 2.2: **Green** — In document mode, set blob directory to `<dir>/_EXTERNAL_DATA/` instead of the app support path.
- [ ] Task 2.3: **Red** — Write test: deleting the model removes the blob from `<dir>/_EXTERNAL_DATA/`.
- [ ] Task 2.4: **Green** — Ensure delete path resolves correctly in document mode.

### Verification

- [ ] Blobs stored inside the document directory
- [ ] Full ExternalFile lifecycle works in document mode

---

## Phase 3: Portability and Cleanup

### Tasks

- [ ] Task 3.1: **Red** — Write test: copy the document directory to a new location, open from there — all data and blobs accessible.
- [ ] Task 3.2: **Green** — Ensure no absolute paths are stored in the database; blob UUIDs are relative to the document directory.
- [ ] Task 3.3: **Red** — Write test: `dispose()` checkpoints WAL — after dispose, `StoreContent-wal` is empty or absent.
- [ ] Task 3.4: **Green** — Add `PRAGMA wal_checkpoint(TRUNCATE)` to `dispose()` in document mode.
- [ ] Task 3.5: **Refactor** — Ensure document mode vs standard mode is cleanly branched in `ModelContainer`.

### Verification

- [ ] Documents are portable (copy and reopen)
- [ ] Clean WAL state after dispose
- [ ] `flutter test` — all tests pass, zero failures

---

## Final Verification

- [ ] All acceptance criteria from spec met
- [ ] Document mode fully functional with database and blobs
- [ ] `flutter test` passes with zero failures

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._

# Implementation Plan: Blob Maintenance (cleanOrphanedBlobs)

**Track ID:** blob-maintenance_20260405
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-05
**Status:** [ ] Pending | [~] In Progress | [x] Complete

## Overview

Replace the stub `cleanOrphanedBlobs()` with a real implementation that scans the blob directory and cross-references against the database.

---

## Phase 1: Core Implementation

### Tasks

- [ ] Task 1.1: **Red** — Write test: insert a Photo with ExternalFile, save, then manually create an extra file in `_EXTERNAL_DATA/`. Call `cleanOrphanedBlobs()` — returns 1, extra file removed, referenced file preserved.
- [ ] Task 1.2: **Green** — Implement `cleanOrphanedBlobs()`: list all files in `_EXTERNAL_DATA/`, query all ExternalFile columns for referenced UUIDs, delete unreferenced files.
- [ ] Task 1.3: **Red** — Write test: `cleanOrphanedBlobs()` with no orphans returns 0 and deletes nothing.
- [ ] Task 1.4: **Green** — Handle the clean case gracefully.
- [ ] Task 1.5: **Red** — Write test: multiple model types with ExternalFile fields — orphans from different models are all cleaned.
- [ ] Task 1.6: **Green** — Iterate over all descriptors with `externalFileFields` to collect referenced UUIDs.

### Verification

- [ ] Orphaned files removed, referenced files preserved
- [ ] Works across multiple model types

---

## Phase 2: Edge Cases

### Tasks

- [ ] Task 2.1: **Red** — Write test: empty `_EXTERNAL_DATA/` directory — returns 0, no crash.
- [ ] Task 2.2: **Green** — Guard against missing directory.
- [ ] Task 2.3: **Red** — Write test: `_EXTERNAL_DATA/` does not exist yet — returns 0.
- [ ] Task 2.4: **Green** — Check directory existence before listing.
- [ ] Task 2.5: **Red** — Write test: calling cleanup while a context has staged (not yet saved) files — staged files are not on disk yet, so nothing to clean.
- [ ] Task 2.6: **Green** — Confirm cleanup only touches actual files on disk.

### Verification

- [ ] Edge cases handled without crashes
- [ ] `flutter test` — all tests pass, zero failures

---

## Final Verification

- [ ] All acceptance criteria from spec met
- [ ] `cleanOrphanedBlobs()` works reliably
- [ ] `flutter test` passes with zero failures

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._

# Dart/Flutter
**/.dart_tool/
**/build/
**/.flutter-plugins
**/.flutter-plugins-dependencies
**/.packages

# build_runner intermediate part files (combining_builder merges these into .g.dart)
**/*.g.part

# Package lock (library — consumers resolve their own)
**/pubspec.lock

# IDE
.idea/
*.iml
.vscode/

# macOS
.DS_Store

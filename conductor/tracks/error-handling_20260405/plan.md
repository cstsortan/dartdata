# Implementation Plan: Error Handling & Edge Cases

**Track ID:** error-handling_20260405
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-05
**Status:** [ ] Pending | [~] In Progress | [x] Complete

## Overview

Add defensive checks throughout the public API surface: read-only enforcement, disposal guards, concurrent context handling, and corruption detection. Each phase targets a specific failure class.

---

## Phase 1: Read-Only Mode

### Tasks

- [ ] Task 1.1: **Red** — Write test: `ModelContext.insert()` on a read-only container throws `StateError`.
- [ ] Task 1.2: **Red** — Write test: `ModelContext.delete()` on a read-only container throws `StateError`.
- [ ] Task 1.3: **Red** — Write test: `ModelContext.save()` on a read-only container throws `StateError`.
- [ ] Task 1.4: **Green** — Add `_ensureWritable()` guard to `insert()`, `delete()`, and `save()` that checks `container.configuration.isReadOnly`.
- [ ] Task 1.5: **Red** — Write test: `fetch()` and `fetchCount()` work on a read-only container.
- [ ] Task 1.6: **Green** — Ensure read operations bypass the writable check.

### Verification

- [ ] Write operations blocked on read-only containers
- [ ] Read operations allowed on read-only containers

---

## Phase 2: Disposal Guards

### Tasks

- [ ] Task 2.1: **Red** — Write test: calling `ModelContext.fetch()` after `container.dispose()` throws `StateError` with "disposed" in message.
- [ ] Task 2.2: **Red** — Write test: calling `ModelContext.insert()` after dispose throws `StateError`.
- [ ] Task 2.3: **Green** — Add `_disposed` flag to `ModelContainer`; set in `dispose()`. Add `_ensureNotDisposed()` guard.
- [ ] Task 2.4: **Green** — Check `container._disposed` at entry to every `ModelContext` public method.
- [ ] Task 2.5: **Red** — Write test: calling `dispose()` twice is a no-op (doesn't throw).
- [ ] Task 2.6: **Green** — Guard against double-dispose.

### Verification

- [ ] All operations throw after disposal
- [ ] Double-dispose is safe

---

## Phase 3: Concurrent Contexts

### Tasks

- [ ] Task 3.1: **Red** — Write test: two `ModelContext` instances on the same container can both `fetch()` simultaneously.
- [ ] Task 3.2: **Green** — Verify reads work concurrently (SQLite WAL allows this).
- [ ] Task 3.3: **Red** — Write test: two contexts insert different rows and save — both writes succeed, no corruption.
- [ ] Task 3.4: **Green** — Verify sequential saves don't interfere with each other.
- [ ] Task 3.5: **Red** — Write test: one context saves while another fetches — fetch returns consistent data.
- [ ] Task 3.6: **Green** — Confirm WAL-mode isolation works correctly.

### Verification

- [ ] Multiple contexts coexist safely
- [ ] No data corruption from concurrent operations

---

## Phase 4: Database Error Handling

### Tasks

- [ ] Task 4.1: **Red** — Write test: opening a path where the file is not a valid SQLite database throws a descriptive error.
- [ ] Task 4.2: **Green** — Catch `SqliteException` during open and wrap in `DatabaseCorruptionError` with the original exception and file path.
- [ ] Task 4.3: **Red** — Write test: opening a standard-mode DB at a non-existent path creates the file automatically.
- [ ] Task 4.4: **Green** — Verify SQLite's default create-if-not-exists behavior.
- [ ] Task 4.5: **Refactor** — Ensure all custom error types (`StateError`, `DatabaseCorruptionError`, `OptimisticLockError`, `SchemaMismatchError`) have clear, actionable messages.

### Verification

- [ ] Corrupt database produces descriptive error
- [ ] Missing file handled gracefully
- [ ] `flutter test` — all tests pass, zero failures

---

## Final Verification

- [ ] All acceptance criteria from spec met
- [ ] API surface is defensive and produces clear errors
- [ ] `flutter test` passes with zero failures

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._

# Implementation Plan: QueryObserver<T> Widget

**Track ID:** query-observer_20260405
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-05
**Status:** [ ] Pending | [~] In Progress | [x] Complete

## Overview

Build `QueryObserver<T>` as a `StatefulWidget` that subscribes to the `ModelContext.changes` stream, filters for relevant table changes, re-runs the query, and rebuilds via `setState()`. The widget lives in `dartdata/lib/src/widgets/` (new directory for Flutter-specific code).

---

## Phase 1: Basic QueryObserver

Build the core widget that runs a query and displays results.

### Tasks

- [ ] Task 1.1: **Red** — Write widget test: `QueryObserver<Trip>(query: Query<Trip>(), builder: ...)` renders with `null` results initially, then shows fetched results after the first frame.
- [ ] Task 1.2: **Green** — Implement `QueryObserver<T>` as a `StatefulWidget`. In `initState()`, run the query and `setState()` with results. Builder receives `null` until query completes.
- [ ] Task 1.3: **Red** — Write test: builder receives the correct number of results after initial query.
- [ ] Task 1.4: **Green** — Wire `ModelContext.fetch(query)` into the state and pass results to builder.
- [ ] Task 1.5: **Red** — Write test: `child` parameter is passed through to builder and does not rebuild.
- [ ] Task 1.6: **Green** — Implement child forwarding in the builder pattern.

### Verification

- [ ] QueryObserver renders with initial query results
- [ ] Widget tests pass

---

## Phase 2: Reactive Re-Querying

Subscribe to changes and re-run the query when the observed table is modified.

### Tasks

- [ ] Task 2.1: **Red** — Write test: after inserting a Trip and calling `save()`, the QueryObserver rebuilds with updated results.
- [ ] Task 2.2: **Green** — In `initState()`, subscribe to `context.changes`. On each event, check `changeSet.hasChangesFor(tableName)`. If true, re-run query and `setState()`.
- [ ] Task 2.3: **Red** — Write test: saving changes to a different table (e.g., BucketListItem) does NOT trigger a rebuild of a `QueryObserver<Trip>`.
- [ ] Task 2.4: **Green** — Ensure the table name filter works correctly. Derive the table name from the model type via the schema.
- [ ] Task 2.5: **Red** — Write test: deleting a Trip triggers a rebuild with fewer results.
- [ ] Task 2.6: **Green** — Verify delete changes are also detected.

### Verification

- [ ] Observer rebuilds on relevant changes
- [ ] Observer does NOT rebuild on irrelevant changes
- [ ] Widget tests pass

---

## Phase 3: Lifecycle and Cleanup

### Tasks

- [ ] Task 3.1: **Red** — Write test: disposing the widget cancels the stream subscription (no further rebuilds after dispose).
- [ ] Task 3.2: **Green** — Cancel `StreamSubscription` in `dispose()`.
- [ ] Task 3.3: **Red** — Write test: passing a `ModelContext` directly (not via `.of()`) works correctly.
- [ ] Task 3.4: **Green** — Support both `context` parameter and `.of()` lookup.
- [ ] Task 3.5: **Refactor** — Export `QueryObserver` from the main barrel file (`dartdata.dart`).

### Verification

- [ ] No memory leaks from stream subscriptions
- [ ] Both context-passing modes work
- [ ] `flutter test` — all tests pass, zero failures

---

## Final Verification

- [ ] All acceptance criteria from spec met
- [ ] `QueryObserver` renders, reacts, and cleans up correctly
- [ ] `flutter test` passes with zero failures
- [ ] Widget exported from public API

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._

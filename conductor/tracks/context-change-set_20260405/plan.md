# Implementation Plan: Full ContextChangeSet

**Track ID:** context-change-set_20260405
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-05
**Status:** [ ] Pending | [~] In Progress | [x] Complete

## Overview

Expand `ContextChangeSet` from a stub into a fully populated structure, collect change records during `save()`, populate the `_change_log` table, and emit meaningful change sets on the stream.

---

## Phase 1: ContextChangeSet Data Model

Define the change set structure and change record types.

### Tasks

- [ ] Task 1.1: **Red** — Write test: `ContextChangeSet` has a `changes` map keyed by table name, containing lists of `ChangeRecord` objects.
- [ ] Task 1.2: **Red** — Write test: `ChangeRecord` has `tableName`, `primaryKey`, and `changeType` (insert/update/delete) fields.
- [ ] Task 1.3: **Green** — Implement `ChangeRecord` class and `ChangeType` enum. Expand `ContextChangeSet` with `Map<String, List<ChangeRecord>> changes` and helper methods (`tablesAffected`, `hasChangesFor(tableName)`).
- [ ] Task 1.4: **Red** — Write test: `ContextChangeSet.hasChangesFor('trip')` returns true when trip table has changes, false otherwise.
- [ ] Task 1.5: **Green** — Implement `hasChangesFor()` and `tablesAffected` getter.

### Verification

- [ ] ContextChangeSet data model is complete and testable
- [ ] Existing stream tests still pass (they don't inspect change set contents)

---

## Phase 2: Collecting Changes During Save

Wire change collection into the save() pipeline.

### Tasks

- [ ] Task 2.1: **Red** — Write test: after insert + save, the emitted `ContextChangeSet` contains one insert record for the correct table and id.
- [ ] Task 2.2: **Green** — In `save()`, collect `ChangeRecord(tableName, id, ChangeType.insert)` for each pending insert. Pass to `_ChangeNotifier.notify()`.
- [ ] Task 2.3: **Red** — Write test: after update + save, emitted change set contains one update record.
- [ ] Task 2.4: **Green** — Collect update records during the update phase of `save()`.
- [ ] Task 2.5: **Red** — Write test: after delete + save, emitted change set contains one delete record.
- [ ] Task 2.6: **Green** — Collect delete records during the delete phase of `save()`.
- [ ] Task 2.7: **Red** — Write test: mixed operations (insert + delete) in one save produce a change set with both record types.
- [ ] Task 2.8: **Green** — Combine all collected records into a single `ContextChangeSet`.

### Verification

- [ ] Change sets accurately reflect all operations in each save
- [ ] `flutter test` — zero failures

---

## Phase 3: Persistent Change Log

Write change records to the `_change_log` table.

### Tasks

- [ ] Task 3.1: **Red** — Write test: after insert + save, `_change_log` table has one row with entity, pk, change_type, and timestamp.
- [ ] Task 3.2: **Green** — In `save()`, after successful transaction, write each `ChangeRecord` to `_change_log` table.
- [ ] Task 3.3: **Red** — Write test: `_change_log` accumulates across multiple saves.
- [ ] Task 3.4: **Green** — Ensure INSERT (not INSERT OR REPLACE) is used for change log entries.
- [ ] Task 3.5: **Refactor** — Ensure change log writes are inside the transaction for atomicity.

### Verification

- [ ] `_change_log` table populated correctly
- [ ] `flutter test` — all tests pass, zero failures

---

## Final Verification

- [ ] All acceptance criteria from spec met
- [ ] `ContextChangeSet` carries per-table, per-operation change records
- [ ] `_change_log` table populated on every save
- [ ] `flutter test` passes with zero failures
- [ ] Existing stream tests remain backward-compatible

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
